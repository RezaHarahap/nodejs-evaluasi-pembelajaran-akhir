const { Buffer } = require('buffer');

/**
 * @TODO
 * 1. Buatlah variabel bernama `buffer` yang merupakan instance dari Buffer dengan panjang 50 byte.
 */

const buffer = Buffer.alloc(50);

module.exports = buffer;
